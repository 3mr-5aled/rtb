# Issue Tracker Configuration

This repository uses **local markdown files** for issue tracking.

- **Location**: `.scratch/<feature>/`
- **Format**: Standard Markdown files representing issues, tasks, and specifications.
- **Workflow**: Engineering skills (`to-spec`, `writing-plans`, `executing-plans`) read and store specs/tasks under `.scratch/` or `docs/superpowers/plans/`.
